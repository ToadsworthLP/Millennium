using System.Collections;
using UnityEngine;

public class ItemCoroutines : ItemUtils {

    //IEnumerator poison(ItemParameter parameter) {
    //    for (int i = 0; i < int.Parse(parameter.args[0]); i++) {
    //        yield return new WaitForSeconds(1);
    //        getBackpack().hp -= 1;
    //    }
    //}
}
